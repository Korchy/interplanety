package Vn.Interface.Button {
	// Класс "Кнопка "Назад"
//-----------------------------------------------------------------------------------------------------
	import Vn.Math.Vector2;
	import Vn.Common.SC;
	import Vn.Objects.VnObjectT;
//-----------------------------------------------------------------------------------------------------
	public class PrevButton extends Button {
//-----------------------------------------------------------------------------------------------------
// Переменные
//-----------------------------------------------------------------------------------------------------
		private var VertAlign:uint;	// Вертикальное выравнивание
//-----------------------------------------------------------------------------------------------------
// Конструктор
//-----------------------------------------------------------------------------------------------------
		public function PrevButton() {
			// Конструктор родителя
			super();
			// Конструктор
			NeedPlace = true;
			Name = 45;
			ReLoadById(12);	// arrowleft
			VAlign = SC.MIDDLE;
		}
//-----------------------------------------------------------------------------------------------------
// Деструктор - вызывать самому, автоматически не вызывается
//-----------------------------------------------------------------------------------------------------
		override public function _delete():void {
			// Деструктор
			
			// Деструктор предка
			super._delete();
		}
//-----------------------------------------------------------------------------------------------------
// Функции
//-----------------------------------------------------------------------------------------------------
		override protected function GetPlace():Vector2 {
			// Возвращает местоположение кнопки
			if(stage!=null) {
				// Если добавлена в список отображения
				switch (VertAlign) {
					case SC.TOP:
							// левый верхний угол парента
						return new Vector2(Width05+1,Height05);
						break;
					case SC.MIDDLE:
							// слева по середине парента
//						trace(VnObjectT(parent).Height05);
						return new Vector2(Width05+1,VnObjectT(parent).Height05);
						break;
					case SC.BOTTOM:
							// левый нижний угол парента
						return new Vector2(Width05+1,VnObjectT(parent).Height-Height05);
						break;
				}
			}
			return new Vector2(0.0,0.0);
		}
//-----------------------------------------------------------------------------------------------------
// Обработка событий
//-----------------------------------------------------------------------------------------------------
	
//-----------------------------------------------------------------------------------------------------
// Методы get/set для установки значений переменных
//-----------------------------------------------------------------------------------------------------
		public function set VAlign(Value:uint):void {
			if(Value<=SC.BOTTOM||Value>=SC.TOP) VertAlign = Value;
		}
//-----------------------------------------------------------------------------------------------------
	}
}